public class CuentaVIP extends Cuenta {
    public CuentaVIP(String dni, String nombre, String numCuenta, double saldo) {
        super(dni, nombre, numCuenta, saldo);
        // Otros atributos específicos de la cuenta VIP
    }
}